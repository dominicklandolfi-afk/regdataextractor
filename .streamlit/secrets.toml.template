# Template for Streamlit Cloud secrets. Do NOT commit the real secrets.toml.
# In Streamlit Cloud: App Settings -> Secrets -> paste the lines below
# (without the "template" extension and with real values).

PERPLEXITY_API_KEY = "pplx-..."
APP_PASSWORD = "WERCS"
